apiVersion: v1
kind: Service
metadata:
  name: svc-gateway
  namespace: planara
spec:
  type: ClusterIP
  selector:
    app: svc-gateway
  ports:
    - name: http
      port: 8080
      targetPort: 8080